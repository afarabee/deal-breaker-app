

## Plan: Add Hammer Icon to "Let's Break My Deal" Button

**File: `src/components/screens/VehicleInfoScreen.tsx`**

- Import `Hammer` from `lucide-react`
- Add `<Hammer className="h-5 w-5" />` inside the Button, before the text "Let's Break My Deal →"

