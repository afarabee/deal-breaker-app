

## Plan: Make Logo Larger and More Prominent

Update `src/components/DealHeader.tsx` — increase logo height from `h-16` (64px) to `h-20` (80px) so it's clearly the most prominent element in the header.

**Change:** Both logo `<img>` tags: `h-16` → `h-20`

